import { queryKeys } from "../utils/constants";
import { useQuery } from "@tanstack/react-query";

function TestPage() {
	return (
		<div className="grid items-center justify-center gap-16 p-4">
			<iconify-icon icon="solar:rounded-magnifer-bug-line-duotone" class="text-8xl" />
		</div>
	);
}

export default TestPage;
